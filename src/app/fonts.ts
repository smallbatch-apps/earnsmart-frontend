import { Inter, Raleway, PT_Sans, Noto_Sans } from "next/font/google";
export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const raleway = Raleway({
  subsets: ["latin"],
  display: "swap",
});

export const ptSans = PT_Sans({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const notoSans = Noto_Sans({
  subsets: ["latin"],
  display: "swap",
});
